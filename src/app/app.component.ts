import { Component } from '@angular/core';

@Component({
  selector: 'app-root',
  template: '<router-outlet></router-outlet>', //le saque el URL para que funcionara
  
})
export class AppComponent {
  
}
